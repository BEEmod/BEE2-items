"Conditions"
	{
	// Wall Grates
	"Condition"
		{
		"instance" "<ITEM_LAUTARO_WALL_GRATES>"
		"Condition"
			{
			"BlockType" // If there is a block at the side of the Standing Fizzler, add a black frame texture inside that fizzler´s frame.
				{
				"Offset" "0 0 1" // X, Y, and Z Axis order.
				"Type" "SOLID EMBED" // The exeption´s list where the visgroup brush must be generated (See https://github.com/BEEmod/BEE2-items/wiki/vbsp_config_conditions#blocktype).
				}		
		    "Result"
			    {
  			"TemplateBrush"
					{
					"ID"        "STANDING_WALL" 
					}
				}
			}		
		}
	}		