unlitgeneric
	{
	// This texture is positioned around the light origin, to ensure the model gets rendered.
	// Otherwise it might vanish, when the game thinks the model is invisible (all under the floor).
	
	// Additive adds the colour to backgrounds - x+0 = x,
	// so this does nothing.
	$color2 "{0 0 0}"
	$additive 1
	$model 1
	}
