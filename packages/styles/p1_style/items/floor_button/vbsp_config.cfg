"Conditions"
	{
	// Portal 1's buttons are always on white platforms, even in black areas.
	// So we don't actually care about the surface type. Still use a template for
	// orientation swapping.
	"Condition"
		{
		"instance" "<ITEM_BUTTON_FLOOR>"
		"Result"
			{
			"TemplateBrush" 
				{
				"ID" "BEE2_FLOOR_BUTTON_BASE_P1"
				}
			}
		}
	}
